name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: [self-hosted, linux]
    timeout-minutes: 15

    steps:
      - name: Pull & build
        working-directory: /var/www/sonnalab.com
        run: |
          git pull origin main
          npm ci
          npm run build
